import styled from "styled-components";

export const ErrorPopupStyled = styled.div`
  padding: 1rem;
  /* margin-top: 1rem; */
  border-radius: 5px;
  background-color: rgba(200, 20, 20, 0.4);
  position: absolute;
  right: 0;
  bottom: 0;
  top: 0;
  display: grid;
  place-content: center;
`;
